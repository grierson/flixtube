{:components
 {:db     {:start (cart.datastore/repository)}
  :router {:start (cart.routes/app (clip/ref :db))}
  :http   {:start (ring.adapter.jetty/run-jetty
                    (clip/ref :router)
                    {:port  3000
                     :join? false})
           :stop  (.stop this)}}}